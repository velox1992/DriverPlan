<Window x:Class="DriverPlan.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodel="clr-namespace:DriverPlan.viewmodel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewmodel:MainWindowViewModel />
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Neu" Command="{Binding CreateNewPlanCommand}" />
            <MenuItem Header="Öffnen" Command="{Binding LoadPlanCommand}" />
            <MenuItem Header="Speichern" Command="{Binding SavePlanCommand}" />
        </Menu>
        <Grid>
            <StackPanel>
                <StackPanel Orientation="Vertical">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Row="0" Grid.Column="0" Content="Fahrer" />
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NewItemName, Mode=TwoWay}" />
                        <Label Grid.Row="1" Grid.Column="0" Content="Notiz" />
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NewItemNote, Mode=TwoWay}" />
                        <Label Grid.Row="2" Grid.Column="0" Content="Datum" />
                        <DatePicker Grid.Row="2" Grid.Column="1" SelectedDate="{Binding NewItemDate, Mode=TwoWay}" />
                    </Grid>
                    <Button Command="{Binding AddNewItemCommand}" Content="Add" />
                </StackPanel>
                <DataGrid AutoGenerateColumns="False" ColumnWidth="*" VerticalScrollBarVisibility="Visible"
                          ItemsSource="{Binding DriverPlanEntries}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fahrer" Binding="{Binding Driver}" />
                        <DataGridTextColumn Header="Notiz" Binding="{Binding Note}" />
                    </DataGrid.Columns>
                </DataGrid>
                <ListView ItemsSource="{Binding Report}">
                </ListView>

            </StackPanel>

        </Grid>
    </DockPanel>
</Window>